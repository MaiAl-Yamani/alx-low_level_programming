#include <stdio.h>
/**
 * main - run program
 * @argc: size of arguments
 * @argv: string array
 *
 * Return: always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
