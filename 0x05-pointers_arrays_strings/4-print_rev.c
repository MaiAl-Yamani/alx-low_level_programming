#include "main.h"
/**
 * print_rev - print a string in reverse
 * @s: pointer to s
 *
 * Return: void
 */
void print_rev(char *s)
{
	int n;
	s[n];

	for (n = -2; *s != '\0'; s--)
	{
		_putchar(*s[n]);
	}

	_putchar('\n');
}
